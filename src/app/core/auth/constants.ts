/** sessionStorage key for the JWT + refresh token pair. */
export const TOKEN_STORAGE_KEY = 'clinicx.auth.tokens';

/** sessionStorage key for the mock SMS code (development only). */
export const MOCK_CODE_KEY = 'clinicx.mock.code';

/** LocalStorage key for the active account ID (migrates to JWT claims later). */
export const ACTIVE_ACCOUNT_KEY = 'clinicx.activeAccountId';
